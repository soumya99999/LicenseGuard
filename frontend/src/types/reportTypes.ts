export interface LicenseReport {
  softwareName: string;
  licenseKey: string;
  purchaseDate: string;
  expiryDate: string;
  totalQuantity: number;
  departmentName: string;
  procuredBy: string;
  assignDate: string;
}
